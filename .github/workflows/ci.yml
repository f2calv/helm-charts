name: ci

on:
  workflow_dispatch:
  push:
    branches-ignore:
      - "preview/**"
    paths-ignore:
      - .github/dependabot.yml
      - LICENSE
      - README.md
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions: {}

jobs:
  lint:
    if: github.ref != format('refs/heads/{0}', github.event.repository.default_branch)
    permissions:
      contents: read
    uses: f2calv/gha-workflows/.github/workflows/lint.yml@v1

  publish:
    name: publish (${{ matrix.chart }})
    strategy:
      fail-fast: false
      matrix:
        chart:
          - workload
    permissions:
      contents: write
      packages: write
    uses: ./.github/workflows/_publish-chart.yml
    with:
      chart: ${{ matrix.chart }}
    secrets: inherit
