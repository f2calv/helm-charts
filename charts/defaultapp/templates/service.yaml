apiVersion: v1
kind: Service
metadata:
  name: {{ include "defaultapp.fullname" . }}
  labels:
    {{- include "defaultapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "defaultapp.selectorLabels" . | nindent 4 }}
