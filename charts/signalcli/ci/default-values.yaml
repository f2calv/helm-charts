# Default install: json-rpc mode with a persistent volume for account state.
signalcli:
  persistentVolumeClaims:
    - name: signalcli-pvc
      accessModes:
        - ReadWriteOnce
      storage: 512Mi
